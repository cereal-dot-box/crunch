# Backend GraphQL API
BACKEND_GRAPHQL_URL=http://localhost:3000/graphql

# Shared secret for JWT signing (must match backend's BETTER_AUTH_SECRET)
# Generate a secure random 64+ character string for production
BETTER_AUTH_SECRET=your-secret-key-change-this-in-production

# Node Environment
NODE_ENV=development
LOG_LEVEL=info
